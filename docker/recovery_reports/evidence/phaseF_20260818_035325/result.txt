FAILED 1 failed (2.0m) - phase9.spec.ts STEP 12G-1, test timeout; hang at line 171 locator.textContent('text=USD') never resolved; killed by 120s timeout (Target page closed = cleanup).
Diagnosis (trace + source): iteration 1 (IDR) resolves because sessionStorage fallback works when localStorage is EMPTY; app store (apps/frontend/src/stores/dashboardCurrency.store.ts) prefers localStorage FIRST (line 18-21), falls back to sessionStorage ONLY when localStorage empty (line 23-24); setCurrency() writes localStorage + REMOVES sessionStorage (line 42-43). After iteration 1, app-side setCurrency writes localStorage='IDR' + clears sessionStorage -> iteration 2 (USD) hydrate reads localStorage='IDR' -> header stays IDR -> text=USD never appears -> hang. phase9 writes/reads sessionStorage (lines 65/89/146-149) = wrong storage key vs app's actual primary key (localStorage, proven working by multi-currency-acceptance.final.spec.ts). waitForResponse /dashboard/summary + /dashboard/widgets also timeout 3s each iteration (endpoints not hit on navigation) - harmless (try/catch). NOT a product defect; pre-existing spec storage-key mismatch.
