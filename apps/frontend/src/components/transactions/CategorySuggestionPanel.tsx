"use client";

// Smart Category Suggestion feature removed per Batch 2 cleanup.
// Keep a lightweight stub export so legacy imports don't crash during transition.
export function CategorySuggestionPanel(): null {
  return null;
}
